PORT=4000
# Interface to listen on.
#   127.0.0.1  only this machine can reach :4000 (safest; enough when cloudflared runs here too)
#   0.0.0.0    both http://localhost:4000 and http://<lan-ip>:4000 answer
# Binding a single LAN address does NOT also cover localhost, so use 0.0.0.0 when both must work.
# Anything other than loopback puts :4000 on the office network, guarded only by SHARED_SECRET.
HOST=127.0.0.1
SHARED_SECRET=change-me
# 9001, not Tally's default 9000: the client's server runs one TallyPrime per Windows
# session, and 9000 is already taken by the instance holding their live books.
TALLY_PORT=9001
# Port to READ master names from, when the live company sits on a different TallyPrime instance
# to the one vouchers are written to. Defaults to TALLY_PORT. Read-only: the import paths never
# use it, so this cannot send a voucher to the live books.
TALLY_MASTERS_PORT=9000

TALLY_TIMEOUT_MS=30000
DEFAULT_COMPANY=YOUR TALLY COMPANY NAME

# TESTING ONLY. TallyPrime's free Educational version rejects any voucher date other than the
# 1st, 2nd, or last day of a month. Setting this rewrites dates to fit, so a rejection you see is
# a real XML problem rather than a date-policy one. Leave OFF against a licensed Tally.
TALLY_EDU_MODE=false

# TESTING ONLY. Creates any ledger the voucher references but Tally does not have yet (party,
# sales, tax, round-off). Off by default: against a real company a mapping typo would silently
# become a new ledger instead of a loud failure, leaving near-duplicate accounts to clean up.
ALLOW_MASTER_CREATE=false

# Ask Tally whether the bill already exists before importing.
#
# OFF because it does not work against this client's Tally: the voucher collection it needs never
# returns, and every push died on it at the 30s timeout without importing anything. Measured — the
# log showed took:30004ms on the duplicate check for every push, with an empty queue.
#
# Duplicates are guarded on the backend instead, which skips invoices already marked synced. The
# gap: an import that succeeds while its reply is lost is recorded as Failed, so retrying it could
# write the voucher twice. Look in Tally before re-pushing a bill that timed out.
TALLY_DUPLICATE_CHECK=false
